import React from "react";
import DataTable from "./DataTable";

const App = () => (
  <div style={{ padding: "20px", backgroundColor: "#222", minHeight: "100vh" }}>
    <h1 style={{ color: "#fff" }}>Data Table with MUI</h1>
    <DataTable />
  </div>
);

export default App;
